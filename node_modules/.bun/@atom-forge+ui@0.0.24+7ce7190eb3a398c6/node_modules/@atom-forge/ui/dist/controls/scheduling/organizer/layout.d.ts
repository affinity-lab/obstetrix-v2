import type { OrganizerItem, OrganizerItemRendered, OverlapStrategy } from './types.js';
export declare function computeLayout(items: OrganizerItem[], cols: number, strategy: OverlapStrategy, rowHeight: number): OrganizerItemRendered[];
export declare function overlaps(a: {
    x: number;
    y: number;
    w: number;
    h: number;
}, b: {
    x: number;
    y: number;
    w: number;
    h: number;
}): boolean;
