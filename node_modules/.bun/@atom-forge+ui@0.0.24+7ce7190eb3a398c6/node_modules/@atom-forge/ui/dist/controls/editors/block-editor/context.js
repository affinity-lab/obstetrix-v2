import { getContext, setContext } from 'svelte';
const KEY = Symbol('block-api');
export function setBlockAPI(api) {
    setContext(KEY, api);
}
export function getBlockAPI() {
    return getContext(KEY);
}
