export const ICON_STROKE_DEFAULT = 4;
export function defineIcon(icon, stroke = ICON_STROKE_DEFAULT) {
    return new IconDefiner(icon, stroke);
}
export class IconDefiner {
    get component() {
        return this._component;
    }
    get stroke() {
        return this._stroke;
    }
    get classes() {
        return this._classes;
    }
    _component;
    _stroke;
    _classes = "";
    constructor(icon, stroke = ICON_STROKE_DEFAULT) {
        if (icon instanceof IconDefiner) {
            this._component = icon.component;
            this._stroke = stroke !== ICON_STROKE_DEFAULT ? stroke : icon.stroke;
            this._classes = icon.classes;
        }
        else {
            this._component = icon;
            this._stroke = stroke;
        }
    }
    class(classes) {
        this._classes = classes;
        return this;
    }
    setStroke(stroke) {
        this._stroke = stroke;
        return this;
    }
}
