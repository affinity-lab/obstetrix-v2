import { getContext, setContext } from 'svelte';
const KEY = Symbol('calendar');
export function setCalendarCtx(ctx) {
    setContext(KEY, ctx);
}
export function getCalendarCtx() {
    return getContext(KEY);
}
