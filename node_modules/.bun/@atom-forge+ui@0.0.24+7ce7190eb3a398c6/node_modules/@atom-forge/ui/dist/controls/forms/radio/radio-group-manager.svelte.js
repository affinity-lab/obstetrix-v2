import { getContext, setContext } from "svelte";
class RadioGroupManager {
    size;
    variant;
    value = $state();
    constructor(initialValue, size, variant) {
        this.size = size;
        this.variant = variant;
        this.value = initialValue;
    }
}
const KEY = 'atom-forge:radio-group-manager';
export function createRadioGroupManager(initialValue, size, variant) {
    const manager = new RadioGroupManager(initialValue, size, variant);
    setRadioGroupManager(manager);
    return manager;
}
export const getRadioGroupManager = () => getContext(KEY);
export const setRadioGroupManager = (manager) => setContext(KEY, manager);
