import type { AnyProp, ClassProp, XOR } from "../../../index";
import { type IconDefinition } from "./icon-definition";
type $$ComponentProps = ClassProp & AnyProp & XOR<{
    size: number | `${number}`;
}, {
    pxSize: number | `${number}`;
}, {}> & {
    icon: IconDefinition;
    stroke?: number | `${number}`;
};
declare const Icon: import("svelte").Component<$$ComponentProps, {}, "">;
type Icon = ReturnType<typeof Icon>;
export default Icon;
