import type { Snippet } from 'svelte';
import type { AnyProp, ClassProp } from '../../../helpers/types';
export type MeterItem = {
    id: string;
    label: string;
    value: number;
    color: string;
};
type $$ComponentProps = ClassProp & AnyProp & {
    items: MeterItem[];
    max?: number;
    vertical?: boolean;
    small?: boolean;
    compact?: boolean;
    segmentClass?: string;
    segment?: Snippet<[MeterItem]>;
    onsegmentclick?: (item: MeterItem) => void;
};
declare const MeterGroup: import("svelte").Component<$$ComponentProps, {}, "">;
type MeterGroup = ReturnType<typeof MeterGroup>;
export default MeterGroup;
