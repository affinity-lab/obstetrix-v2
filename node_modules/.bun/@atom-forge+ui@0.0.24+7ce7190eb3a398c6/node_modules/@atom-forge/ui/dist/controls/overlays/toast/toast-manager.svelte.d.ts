import { type Component } from 'svelte';
import { type IconDefinition } from "../../general/icon";
export type ToastType = 'info' | 'success' | 'warning' | 'error';
export type ToastOptions = {
    type?: ToastType;
    duration?: number;
    closable?: boolean;
    icon?: IconDefinition;
    action?: {
        label: string;
        callback: () => void;
    };
};
export type ToastState = {
    id: string;
    message: string;
    options?: ToastOptions;
    component?: Component;
    props?: Record<string, any>;
};
declare class ToastManager {
    toasts: ToastState[];
    show(message: string, options?: ToastOptions): string;
    dismiss(id: string): void;
    showCustom(component: Component, props: Record<string, any>, options?: ToastOptions): string;
}
export declare function createToastManager(): ToastManager;
export declare const getToastManager: () => ToastManager;
export declare const setToastManager: (manager: ToastManager) => ToastManager;
export {};
