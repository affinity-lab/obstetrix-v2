import { RenderSnippet } from "../../../index";
import { getContext, setContext } from "svelte";
export class PopupManager {
    parent;
    popup = $state(undefined);
    ignoreClose = false;
    closeTimeout = null;
    constructor(parent) {
        this.parent = parent;
    }
    open = {
        snippet: (snippet, params, args, ref) => this.openPopup(RenderSnippet, { snippet: snippet, args: params }, args.pos, args.anchor, args.align, args.offset, ref),
        component: (component, params, args, ref) => this.openPopup(component, params, args.pos, args.anchor, args.align, args.offset, ref)
    };
    openPopup(component, params, event, anchor, align = 'auto', offset, ref) {
        if (this.popup && ref !== undefined && this.popup?.ref === ref) {
            return this.popup.promise;
        }
        if (this.popup)
            this.close();
        this.ignoreClose = true;
        setTimeout(() => this.ignoreClose = false, 100);
        let resolver = () => { };
        const promise = new Promise((resolve) => { resolver = resolve; });
        if (anchor) {
            if ('currentTarget' in anchor) {
                if (anchor.currentTarget === null)
                    throw new Error("Could not resolve popup anchor.");
                if (!(anchor.currentTarget instanceof Element))
                    throw new Error("Could not resolve popup anchor.");
                anchor = anchor.currentTarget;
            }
        }
        setTimeout(() => this.popup = { component, params, resolver, promise, event, anchor, align, offset, ref }, this.popup ? 200 : 0);
        return promise;
    }
    close() {
        if (this.popup && this.closeTimeout === null && !this.ignoreClose) {
            this.closeTimeout = window.setTimeout(() => {
                this.popup?.resolver(undefined);
                this.popup = undefined;
                this.closeTimeout = null;
            }, 10);
        }
    }
    closeRoot() {
        let rootManager = this;
        while (rootManager.parent)
            rootManager = rootManager.parent;
        rootManager.close();
    }
    resolve(result) {
        this.popup?.resolver(result);
        this.popup = undefined;
        this.closeTimeout = null;
    }
    resolveRoot(result) {
        let rootManager = this;
        while (rootManager.parent)
            rootManager = rootManager.parent;
        rootManager.resolve(result);
    }
}
const POPUP_MANAGER_KEY = 'atom-forge:popup-manager';
export function setPopupManager(popupManager) { setContext(POPUP_MANAGER_KEY, popupManager); }
export function getPopupManager() { return getContext(POPUP_MANAGER_KEY); }
export function createPopupManager(parent) {
    let popupManager = new PopupManager(parent);
    setPopupManager(popupManager);
    return popupManager;
}
