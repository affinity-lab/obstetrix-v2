import type { ClassProp, XOR } from "../../../helpers/types";
type $$ComponentProps = ClassProp & XOR<{}, {
    compact: true;
}, {
    small: true;
}> & {
    total: number;
    pageSize: number;
    page?: number;
};
declare const PaginationSlider: import("svelte").Component<$$ComponentProps, {}, "page">;
type PaginationSlider = ReturnType<typeof PaginationSlider>;
export default PaginationSlider;
