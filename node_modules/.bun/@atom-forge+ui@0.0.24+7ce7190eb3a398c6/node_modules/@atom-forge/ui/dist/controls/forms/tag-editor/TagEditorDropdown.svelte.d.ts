import type { Snippet } from 'svelte';
import { type FlatItem } from '../../../helpers/SelectPopup.svelte';
type SharedState = {
    items: FlatItem[];
    query: string;
};
type $$ComponentProps = {
    sharedState: SharedState;
    allowNew: boolean;
    onAdd: (tag: string) => void;
    option?: Snippet<[string, boolean]>;
};
declare const TagEditorDropdown: import("svelte").Component<$$ComponentProps, {}, "">;
type TagEditorDropdown = ReturnType<typeof TagEditorDropdown>;
export default TagEditorDropdown;
