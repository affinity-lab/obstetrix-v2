const asFn = (v) => v;
asFn.string = (v) => v;
asFn.number = (v) => v;
asFn.boolean = (v) => v;
asFn.unknown = (v) => v;
asFn.any = (v) => v;
asFn.array = (v) => v;
asFn.object = (v) => v;
asFn.function = (v) => v;
export const as = asFn;
